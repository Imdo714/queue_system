> # 2. Analysis

## YU Queue System


| Category | Information |
| :--- | :--- |
| **Student No** | `22413543` |
| **Name** | `임도현` |
| **E-mail** | `gusehdla@naver.com` |
| **GitHub repository** | `https://github.com/Imdo714/queue_system` |

---
> # 1. Introduction
매년 반복되는 수강신청 기간의 극심한 트래픽 집중은 대학 행정 시스템의 고질적인 불안정성을 초래합니다. 본 프로젝트는 이러한 기술적 한계를 Redis 기반의 고가용성 대기열 설계를 통해 극복하고자 기획되었습니다. 지난 개념화(`Conceptualization`) 단계에서 비즈니스 목적과 시스템의 큰 틀을 정의했다면 

이번 보고서에서는 Use Case 분석을 통해 학생과 관리자, 그리고 시스템 스케줄러 간의 상호작용을 상세화하고, Domain 및 Application 분석을 통해 대기열 아키텍처의 핵심 구조를 정의합니다. 또한, 사용자가 자신의 대기 순번을 실시간으로 확인하며 전략적 판단을 내릴 수 있도록 합니다. UML을 활용한 정밀한 분석 과정을 통해, 시스템이 초당 5,000건의 요청 속에서도 어떻게 정합성을 유지하며 안정적으로 작동하는지 그 구체적인 메커니즘을 이해할 수 있을 것입니다.

---
> # 2. Use case analysis

> # 2.1 Use case diagram

<img width="774" height="531" alt="Image" src="https://github.com/user-attachments/assets/87f92328-4a83-4041-b677-f368cece815e" />

> # 2.2 Use case description

> ## 2.2.1 Use Case #1: Register Member (회원가입)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 신규 사용자가 학번 정보를 기반으로 시스템 계정을 생성하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 시스템에 등록되지 않은 신규 사용자여야 함 |
| **Trigger (트리거)** | 회원가입 페이지에서 정보 입력 후 '가입' 버튼 클릭 시 |
| **Success Post Condition (성공 후 조건)** | 사용자 정보가 DB에 저장되어 로그인이 가능한 상태가 됨 |
| **Failed Post Condition (실패 후 조건)** | 가입 실패 시 계정이 생성되지 않음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 회원가입 페이지에 접속한다. |
| 2 | 학번, 이름, 이메일, 비밀번호를 입력하고 가입 버튼을 누른다. |
| 3 | 시스템은 입력된 학번이 기존 데이터베이스에 존재하는지 확인한다. |
| 4 | 중복이 없음을 확인하고 사용자 정보를 데이터베이스에 저장한다. |
| 5 | 가입 성공 메시지를 띄우고 로그인 페이지로 전환한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 필수 항목을 입력하지 않은 경우 "모든 항목을 입력해주세요" 메시지를 띄운다. |
| 2 | 이미 등록된 학번인 경우 "이미 존재하는 계정입니다" 메시지를 띄운다. |

---

> ## 2.2.2 Use Case #2: Login (로그인)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 등록된 사용자가 학번과 비밀번호를 통해 시스템 권한을 획득하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생), Administrator (관리자) |
| **Preconditions (사전 조건)** | 시스템에 회원가입이 완료된 상태여야 함 |
| **Trigger (트리거)** | 로그인 페이지에서 정보 입력 후 '로그인' 버튼 클릭 시 |
| **Success Post Condition (성공 후 조건)** | 세션 또는 토큰이 발급되어 수강신청 권한을 획득함 |
| **Failed Post Condition (실패 후 조건)** | 회원인증 실패로 시스템 기능을 사용할 수 없음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 로그인 페이지에 접속한다. |
| 2 | 학번과 비밀번호를 입력하고 로그인 버튼을 누른다. |
| 3 | 데이터베이스에서 해당 사용자의 정보를 조회하고 비밀번호를 검증한다. |
| 4 | 검증 결과가 일치하면 로그인에 성공한다. |
| 5 | 세션/토큰을 발급하고 메인 화면으로 이동한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 비밀번호가 틀린 경우 "비밀번호가 일치하지 않습니다" 메시지를 띄운다. |
| 2 | 존재하지 않는 학번인 경우"존재하지 않는 사용자입니다" 메시지를 띄운다. |

---

> ## 2.2.3 Use Case #3: Enter Queue (대기열 진입)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 수강신청 버튼 클릭 시 Redis 대기열에 진입하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 로그인이 완료된 상태이며 수강신청 기간이어야 함 |
| **Trigger (트리거)** | 수강신청 버튼을 클릭했을 때 |
| **Success Post Condition (성공 후 조건)** | Redis Sorted Set에 접속 기록이 생성되고 대기 순번을 부여받음 |
| **Failed Post Condition (실패 후 조건)** | 대기열 진입에 실패하여 수강신청이 되지 않음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 강의 목록에서 수강신청 버튼을 클릭한다. |
| 2 | 시스템은 Redis를 통해 사용자의 중복 진입 여부를 확인한다. |
| 3 | 중복이 아니라면 Redis Sorted Set에 대기열 데이터(Score)를 저장한다. |
| 4 | 현재 대기 순번을 조회하여 사용자에게 반환한다. |
| 5 | 사용자 화면에 실시간 대기 순번을 표시한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 이미 대기열에 존재하는 경우 "이미 대기 중입니다" 메시지와 현재 순번을 보여준다. |
| 2 | Redis 서버 장애 발생 시 "시스템 오류로 다시 시도해주세요" 메시지를 띄운다. |

---

> ## 2.2.4 Use Case #4: Search Course (강의 조회)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 학생이 개설된 강의 목록과 실시간 잔여석을 조회하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 로그인이 완료된 상태여야 함 |
| **Trigger (트리거)** | 강의 목록 페이지 접속 또는 새로고침 시 |
| **Success Post Condition (성공 후 조건)** | 사용자가 최신 강의 정보 및 잔여석을 확인함 |
| **Failed Post Condition (실패 후 조건)** | 시스템 오류로 인해 강의 목록 조회가 되지 않음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 강의 목록 메뉴를 클릭한다. |
| 2 | 시스템은 DB에서 강의 기본 정보를, Redis에서 잔여석 정보를 조회한다. |
| 3 | 두 정보를 결합하여 사용자에게 강의 목록을 리스트 형태로 출력한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 네트워크 문제로 Redis 조회가 실패한 경우 잔여석 정보를 '확인 불가'로 표시하고 강의 목록만 보여준다. |

---

> ## 2.2.5 Use Case #5: My Page (내 수강신청 내역 조회)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 사용자가 수강신청 내역과 현재 대기 중인 과목의 상태를 확인하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 로그인이 완료된 상태여야 함 |
| **Trigger (트리거)** | 마이페이지 메뉴를 클릭했을 때 |
| **Success Post Condition (성공 후 조건)** | 사용자가 본인의 신청 내역과 대기 상태를 정확히 확인함 |
| **Failed Post Condition (실패 후 조건)** | 정보 조회에 실패하여 데이터를 보여주지 못함 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 마이페이지 메뉴를 클릭한다. |
| 2 | 시스템은 DB에서 사용자의 확정된 수강신청 내역을 조회한다. |
| 3 | 시스템은 Redis에서 사용자의 대기열 진입 여부 및 순번을 조회한다. |
| 4 | 수강신청 내역과 대기 상태 정보를 통합하여 화면에 출력한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 신청 내역이 없는 경우 "신청한 내역이 없습니다"라는 메시지를 표시한다. |
| 2 | 대기열에 존재하지 않는 경우 대기 상태를 "없음"으로 표시한다. |

---

> ## 2.2.6 Use Case #6: Inform Rank (실시간 순번 조회)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 대기열 진입 후 내 앞에 남은 대기 인원을 실시간으로 확인하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-24 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 수강신청 대기열(Redis)에 진입한 상태여야 함 |
| **Trigger (트리거)** | 대기열 진입 후 페이지 유지 또는 새로고침 시 |
| **Success Post Condition (성공 후 조건)** | 사용자가 현재 대기 순번을 명확히 인지함 |
| **Failed Post Condition (실패 후 조건)** | 순번 조회에 실패하여 대기 상태를 확인할 수 없음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 대기열 대기 화면에 머무른다. |
| 2 | 시스템은 Redis Sorted Set의 `ZRANK` 명령어를 통해 사용자의 순위를 조회한다. |
| 3 | 조회된 순위를 사용자에게 1초 단위로 업데이트하여 보여준다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 사용자가 대기열에서 삭제된 경우 (취소 또는 처리 완료) "대기열에서 나왔습니다" 메시지 또는 결과 화면을 보여준다. |
| 2 | Redis 연결이 끊긴 경우 "순번 정보를 불러올 수 없습니다. 다시 시도하세요" 메시지를 띄운다. |

---

> ## 2.2.7 Use Case #7: Strategic Cancel (전략적 취소)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 대기 순번이 너무 뒤에 있을 때 사용자가 스스로 대기를 취소하여 시스템 자원을 확보하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | User level (사용자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-25 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Student (학생) |
| **Preconditions (사전 조건)** | 수강신청 대기열에 진입한 상태여야 함 |
| **Trigger (트리거)** | '대기 취소' 버튼 클릭 시 |
| **Success Post Condition (성공 후 조건)** | 사용자가 대기열에서 제거되고 Redis 리소스가 즉시 반환됨 |
| **Failed Post Condition (실패 후 조건)** | 취소 처리 실패 시 대기열에 유지됨 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 사용자가 대기 순번을 확인하고 취소 버튼을 클릭한다. |
| 2 | 시스템은 Redis Sorted Set에서 해당 사용자의 데이터를 `ZREM` 명령어로 제거한다. |
| 3 | 대기열에서 제거되었다는 확인 메시지를 사용자에게 전송한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 이미 처리 완료되어 대기열에 없는 경우 "이미 처리된 건입니다" 메시지를 띄운다. |

---

> ## 2.2.8 Use Case #8: Batch Registration (배치 승인 처리)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 대기열 상위 사용자를 추출하여 메인 DB에 수강 내역을 일괄 등록하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | System level (시스템 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-25 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | System (Scheduler) |
| **Preconditions (사전 조건)** | 대기열에 사용자가 존재해야 함 |
| **Trigger (트리거)** | 설정된 스케줄러 주기마다 실행 |
| **Success Post Condition (성공 후 조건)** | 대기열 사용자가 DB에 성공적으로 등록됨 |
| **Failed Post Condition (실패 후 조건)** | DB 등록 실패 시 대기열에 다시 유지하거나 실패 처리함 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 스케줄러가 설정된 주기(예: 1초)마다 Redis 대기열에서 상위 N명을 추출한다. |
| 2 | 추출된 사용자의 수강신청 가능 여부를 검증한다. |
| 3 | 메인 DB에 수강신청 데이터를 트랜잭션 단위로 삽입한다. |
| 4 | Redis에서 처리된 사용자를 제거하고 결과 알림을 발송한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | DB 쓰기 중 오류 발생 (예: 정원 초과, 중복) 해당 사용자를 실패 처리한다. |

---

> ## 2.2.9 Use Case #9: Check Duplicate (중복 검증)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 실제 수강 신청 시 동일 과목 중복 또는 시간표 겹침을 검증하는 로직 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | System level (시스템 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-25 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | System |
| **Preconditions (사전 조건)** | 배치 등록 단계에서 호출됨 |
| **Trigger (트리거)** | 수강 신청 데이터 삽입 직전 |
| **Success Post Condition (성공 후 조건)** | 검증 통과 후 DB 등록이 진행됨 |
| **Failed Post Condition (실패 후 조건)** | 검증 실패 시 등록 거부 및 안내 메시지 생성 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 시스템이 신청하려는 과목의 정보를 조회한다. |
| 2 | 기존에 신청된 과목 목록과 시간표 정보를 가져온다. |
| 3 | 중복 여부 및 시간표 충돌 여부를 비교 연산한다. |
| 4 | 조건에 부합할 경우 다음 단계(DB 등록)로 진행한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 중복 신청인 경우 "이미 신청한 과목입니다" 에러를 반환한다. |
| 1 | 시간표 충돌 발생 시 "시간표가 겹칩니다" 에러를 반환한다. |

---

> ## 2.2.10 Use Case #10: Result Notification (신청 결과 알림)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 배치 처리 완료 후 신청 결과를 학생에게 실시간으로 피드백하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | System level (시스템 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-25 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | System (System) |
| **Preconditions (사전 조건)** | 배치 등록(Batch Registration) 프로세스가 완료되어야 함 |
| **Trigger (트리거)** | 배치 결과가 DB에 반영된 직후 |
| **Success Post Condition (성공 후 조건)** | 학생이 알림을 통해 신청 결과를 확인함 |
| **Failed Post Condition (실패 후 조건)** | 알림 전송 실패 시 로그 기록 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 시스템이 배치를 통해 최종 처리된 결과(성공/실패)를 확인한다. |
| 2 | 처리 결과를 사용자 알림 DB에 저장하고, 실시간 통지 서비스를 호출한다. |
| 3 | 학생 화면에 신청 성공/실패 여부를 팝업 또는 알림창으로 표시한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 실시간 통신 연결이 끊긴 경우 학생이 마이페이지에 접속할 때 결과를 조회하도록 알림 상태를 '미확인'으로 남겨둔다. |

---

> ## 2.2.11 Use Case #11: Course Registration (강의 등록)

## GENERAL CHARACTERISTICS (일반적 특성)

| 항목 | 내용 |
| :--- | :--- |
| **Summary (요약)** | 관리자가 신규 강의를 시스템에 등록하는 기능 |
| **Scope (범위)** | YU Queue System |
| **Level (수준)** | Administrative level (관리자 수준) |
| **Author (작성자)** | 임도현 |
| **Last Update (최종 수정일)** | 2026-04-25 |
| **Status (상태)** | Analysis (분석) |
| **Primary Actor (주요 액터)** | Administrator (관리자) |
| **Preconditions (사전 조건)** | 관리자 계정으로 로그인되어 있어야 함 |
| **Trigger (트리거)** | '강의 등록' 메뉴 실행 시 |
| **Success Post Condition (성공 후 조건)** | 강의 정보가 시스템에 저장되어 학생 조회가 가능해짐 |
| **Failed Post Condition (실패 후 조건)** | 등록 실패 시 강의 데이터 저장되지 않음 |

## MAIN SUCCESS SCENARIO (주요 성공 시나리오)

| Step (단계) | Action (행동) |
| :--- | :--- |
| 1 | 관리자가 강의명, 코드, 정원 등 필수 정보를 입력한다. |
| 2 | 시스템은 입력된 데이터의 형식과 필수값 존재 여부를 검증한다. |
| 3 | 데이터베이스에 강의 정보를 저장하고, Redis에 잔여석 정보를 초기화한다. |

## EXTENSION SCENARIOS (확장 시나리오)

| Step (단계) | Branching Action (분기 행동) |
| :--- | :--- |
| 1 | 필수 정보가 누락된 경우 "필수 항목을 모두 입력해주세요" 메시지를 띄운다. |

---

