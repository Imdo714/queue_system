# 3. Design

## YU Queue System

| Category | Information |
| :--- | :--- |
| **Student No** | `22413543` |
| **Name** | `임도현` |
| **E-mail** | `gusehdla@naver.com` |
| **GitHub repository** | `https://github.com/Imdo714/queue_system` |

---
# 1. Introduction

이번 설계(Design) 보고서는 그 분석 결과를 바탕으로, 정의된 기능들을 ‘어떻게 기술적으로 구현하고 최적화할 것인가’를 다루는 문서입니다

본 시스템의 핵심인 고가용성 대기열을 안정적으로 구현하기 위해, 보고서 전반에 걸쳐 Class Diagram을 통한 구조 설계, Sequence Diagram을 이용한 서버-클라이언트 간의 실시간 메시징 흐름, 그리고 대기열의 상태 변화를 다루는 State Machine Diagram을 기술했습니다. 

본 프로젝트는 Kotlin 기반의 Spring Boot 프레임워크를 사용하며, 확장성과 유지보수성을 고려하여 Port-and-Adapter(Hexagonal) 아키텍처를 지향합니다. 비록 인프라 구성의 세부적인 설정은 문서의 범위를 고려하여 요약했으나, 기술된 다이어그램들을 통해 대규모 요청이 유입될 때 시스템이 어떤 메커니즘으로 부하를 분산하고 정합성을 유지하는지 명확히 이해할 수 있을 것입니다.

---
# 2. Class diagram

<img width="2086" height="1388" alt="Image" src="https://github.com/user-attachments/assets/acb78744-fe30-42d0-b7a6-857dc13ba3b4" />

> `User`, `Course`, `Registration` 도메인 객체에 get, set 메서드는 생략하였다.

## 1) User

### (1) Attributes
- `id: Long` : 사용자 고유 식별자
- `studentNo: Long` : 학번
- `password: String` : 로그인 비밀번호
- `name: String` : 사용자 이름
- `role: Role` : 사용자 권한 (예: ADMIN, STUDENT)

### (2) Methods
- `+create(String, String, String): User` : 이름, 학번, 비밀번호를 받아 User 객체를 생성하는 정적 팩토리 메서드

---

## 2) Course

### (1) Attributes
- `id: Long` : 강좌 고유 식별자
- `courseCode: String` : 강좌 코드 (고유 식별 코드)
- `title: String` : 강좌 제목
- `maxCapacity: int` : 최대 수강 인원
- `currentEnrolled: int` : 현재 수강 신청 인원
- `status: CourseStatus` : 강좌 상태 (예: OPEN, CLOSED)
- `dayOfWeek: DayOfWeek` : 수업 요일
- `startTime: LocalTime` : 수업 시작 시간
- `endTime: LocalTime` : 수업 종료 시간

### (2) Methods
- `+create(CreateCourseRequest): Course` : 강좌 생성 요청 DTO를 받아 Course 객체를 생성하는 정적 팩토리 메서드
- `+increaseEnrolled()` : 현재 수강 인원을 1 증가 (수강 신청 시 호출)
- `+decreaseEnrolled()` : 현재 수강 인원을 1 감소 (수강 취소 시 호출)

---

## 3) Registration

### (1) Attributes
- `id: Long` : 수강 신청 고유 식별자
- `user: User` : 수강 신청한 사용자 (User 객체 참조)
- `course: Course` : 수강 신청한 강좌 (Course 객체 참조)
- `registrationDate: LocalDateTime` : 수강 신청 일시

### (2) Methods
- `+create(User, Course): Registration` : User와 Course를 받아 Registration 객체를 생성하는 정적 팩토리 메서드

---

## 4) CourseController

### (1) Attributes
- `courseUseCase: CourseUseCase` : 강좌 관련 유스케이스 주입

### (2) Methods
- `+getCourses(): BaseResponse<List<CourseResponse>>` : 전체 강좌 목록을 조회하여 반환
- `+getCourse(): BaseResponse<List<CourseResponse>>` : 특정 강좌 정보를 조회하여 반환

---

## 5) AdminCourseController

### (1) Attributes
- `createCourseUseCase: CreateCourseUseCase` : 강좌 생성 유스케이스 주입
- `deleteCourseUseCase: DeleteCourseUseCase` : 강좌 삭제 유스케이스 주입

### (2) Methods
- `+createCourse(CreateCourseRequest): BaseResponse<>` : 관리자가 새 강좌를 생성, 생성 결과 반환
- `+deleteCourse(Long, DeleteCourseRequest): BaseResponse<>` : 관리자가 강좌를 삭제, 삭제 결과 반환

---

## 6) CourseService

### (1) Attributes
- `coursePort: CoursePort` : 강좌 영속성 포트 주입
- `userPort: UserPort` : 사용자 영속성 포트 주입

### (2) Methods
- `+createCourse(CreateCourseRequest)` : 강좌 생성 요청을 받아 새 강좌를 저장
- `+deleteCourse(String, Long)` : 강좌 코드와 ID를 받아 강좌를 삭제
- `+getCourses(): List<CourseResponse>` : 전체 강좌 목록을 조회하여 반환
- `+getCourse(Long): CourseResponse` : 특정 강좌 ID로 단건 강좌 정보를 조회하여 반환
- `+validateAdmin(String)` : 토큰을 검증하여 관리자 권한 여부를 확인

---

## 7) CoursePersistenceAdapter

### (1) Attributes
- `courses: ConcurrentHashMap<Long, Course>` : 강좌 ID를 키로 Course 객체를 저장하는 인메모리 저장소
- `courseCodes: ConcurrentHashMap<String, Long>` : 강좌 코드를 키로 강좌 ID를 저장하는 인덱스용 맵

### (2) Methods
- `+save(Course): Course` : Course 객체를 저장소에 저장하고 반환
- `+findById(Long): Course` : 강좌 ID로 Course 객체를 조회
- `+findByCourseCode(String): Course` : 강좌 코드로 Course 객체를 조회
- `+existsByCourseCode(String): Boolean` : 강좌 코드 존재 여부를 확인
- `+delete(Long): void` : 강좌 ID에 해당하는 Course 객체를 삭제

---

## 8) UserController

### (1) Attributes
- `userUseCase: UserUsecase` : 사용자 관련 유스케이스 주입

### (2) Methods
- `+register(RegisterRequest): BaseResponse<>` : 회원가입 요청을 받아 처리하고 결과 반환
- `+login(LoginRequest): BaseResponse<LoginResponse>` : 로그인 요청을 받아 처리하고 토큰/응답 반환

---

## 9) UserService

### (1) Attributes
- `userPort: UserPort` : 사용자 영속성 포트 주입

### (2) Methods
- `+register(RegisterRequest)` : 회원가입 요청을 받아 User를 생성하고 저장
- `+login(LoginRequest): LoginResponse` : 로그인 요청을 받아 인증 후 토큰 등 응답 반환

---

## 10) UserPersistenceAdapter

### (1) Attributes
- `users: ConcurrentHashMap<String, User>` : 학번(studentNo)을 키로 User 객체를 저장하는 인메모리 저장소

### (2) Methods
- `+save(User): User` : User 객체를 저장소에 저장하고 반환
- `+findByStudentNo(String): User` : 학번으로 User 객체를 조회
- `+existsByStudentNo(String): Boolean` : 학번 존재 여부를 확인

---

## 11) RegistrationController

### (1) Attributes
- `createRegistrationUseCase: CreateRegistrationUseCase` : 수강 신청 생성 유스케이스 주입
- `getRegistrationUseCase: GetRegistrationUseCase` : 수강 신청 조회 유스케이스 주입
- `cancelRegistrationUseCase: CancelRegistrationUseCase` : 수강 신청 취소 유스케이스 주입

### (2) Methods
- `+register(CreateRegistrationUseCase): String` : 수강 신청 요청을 받아 처리하고 결과 문자열 반환
- `+getMyRegistrations(String): BaseResponse<List<RegistrationResponse>>` : 내 수강 신청 목록을 조회하여 반환
- `+cancelRegistration(Long, CancelRegistrationRequest): BaseResponse<String>` : 수강 신청 ID와 요청을 받아 취소 처리 후 결과 반환

---

## 12) RegistrationService

### (1) Attributes
- `registrationCommandPort: RegistrationCommandPort` : 수강 신청 Command 포트 주입
- `registrationQueryPort: RegistrationQueryPort` : 수강 신청 Query 포트 주입
- `registrationRedisPort: RegistrationRedisPort` : 수강 신청 Redis 포트 주입
- `registrationValidator: RegistrationValidator` : 수강 신청 유효성 검증 객체 주입
- `courseEnrollmentManager: CourseEnrollmentManager` : 수강 인원 관리 객체 주입

### (2) Methods
- `+register(CreateRegistrationRequest): String` : 수강 신청 요청을 받아 검증 후 신청을 저장하고 결과 반환
- `+getMyRegistrations(String): List<RegistrationResponse>` : 사용자 토큰으로 본인의 수강 신청 목록을 조회하여 반환
- `+cancelRegistration(String, Long): String` : 사용자 토큰과 수강 신청 ID로 수강 신청을 취소하고 결과 반환

---

## 13) RegistrationValidator

### (1) Attributes
- `userPort: UserPort` : 사용자 조회를 위한 영속성 포트
- `coursePort: CoursePort` : 강좌 조회를 위한 영속성 포트
- `registrationQueryPort: RegistrationQueryPort` : 수강 신청 조회를 위한 Query 포트

### (2) Methods
- `+validateAndGetUser(String): User` : 토큰으로 사용자를 조회하고 유효하지 않으면 예외 발생
- `+validateAndGetCourse(Long): Course` : 강좌 ID로 강좌를 조회하고 존재하지 않으면 예외 발생
- `+validateCourseIsOpen(Course)` : 강좌 상태가 OPEN인지 검증, 아니면 예외 발생
- `+validateNotDuplicated(Long, Long)` : 동일 사용자가 동일 강좌에 중복 신청하지 않았는지 검증
- `+ValidateHasCapacity(Course)` : 강좌 수강 인원이 최대 정원을 초과하지 않는지 검증
- `+validateAndGetRegistration(Long, Long): Registration` : 사용자 ID와 수강 신청 ID로 본인의 수강 신청을 조회하고 반환
- `+validateNoTimeConflict(Long, Course)` : 이전 강의 수강 날짜가 같으면 시간 겹치는지 검증

---

## 14) CourseEnrollmentManager

### (1) Attributes
- `coursePort: CoursePort` : 강좌 저장을 위한 영속성 포트

### (2) Methods
- `+increase(Course)` : 강좌의 현재 수강 인원을 1 증가시키고 저장 (수강 신청 확정 시 호출)
- `+decrease(Course)` : 강좌의 현재 수강 인원을 1 감소시키고 저장 (수강 취소 시 호출)

---
## 15) RegistrationPersistenceAdapter

### (1) Attributes
- `store: ConcurrentHashMap` : 수강 신청 ID를 키로 Registration 객체를 저장하는 인메모리 저장소 
- `idSequence: AtomicLong` : 수강 신청 ID 자동 증가 시퀀스

### (2) Methods
- `+save(Registration): Registration` : Registration 객체를 저장소에 저장하고 반환
- `+delete(Long)` : 수강 신청 ID에 해당하는 Registration 객체를 삭제
- `+findById(Long): Registration?` : 수강 신청 ID로 Registration 객체를 조회 *(nullable 변경)*
- `+findByUserIdAndCourseId(Long, Long): Registration?` : 사용자 ID와 강좌 ID로 수강 신청을 조회 *(nullable 변경)*
- `+findByUserId(Long): List` : 사용자 ID로 해당 사용자의 모든 수강 신청 목록을 조회

---
## 16) RegistrationRedisPersistenceAdapter

### (1) Attributes
- `redisTemplate: RedisTemplate` : Redis 연산을 위한 템플릿
- `objectMapper: ObjectMapper` : 객체 직렬화/역직렬화를 위한 매퍼

### (2) Methods
### (2) Methods
- `+joinQueue(Long, String, Int): Long` : 대기열에 수강 신청 등록
- `+removeFromQueue(Long, String): Boolean` : 대기열에서 수강 신청 제거
- `+getQueueRank(Long, String): Long?` : 대기열 내 순위 조회
- `+getQueueSize(Long): Long` : 대기열 전체 크기 조회
- `+getCachedRegistrations(String): List?` : 캐시된 수강 신청 목록 조회
- `+cacheRegistrations(String, List)` : 수강 신청 목록을 캐시에 저장
- `+invalidateRegistrationCache(String)` : 수강 신청 캐시 무효화

---
## 17) RedisConfig

### (1) Attributes
- `host: String` : Redis 서버 호스트
- `port: Int` : Redis 서버 포트
- `password: String` : Redis 인증 비밀번호

### (2) Methods
- `+redisConnectionFactory(): RedisConnectionFactory` : Redis 연결 팩토리 빈 생성
- `+redisTemplate(): RedisTemplate` : RedisTemplate 빈 생성

---
## 18) BaseResponse\<T>

### (1) Attributes
- `code: Int` : HTTP 상태 코드 숫자값 (예: 200, 400)
- `status: HttpStatus` : Spring의 HTTP 상태
- `message: String` : 응답 메시지 (기본값: status.name)
- `data: T?` : 응답 데이터 (기본값: null)

### (2) Methods
- `+of(status: HttpStatus, message: String?, data: T?): BaseResponse<T>` : HTTP 상태, 메시지, 데이터를 받아 BaseResponse 객체를 생성하는 정적 팩토리 메서드. message가 null이면 status.name을 사용
- `+ok(data: T?): BaseResponse<T>` : HTTP 200 OK 상태로 BaseResponse 객체를 생성하는 편의 메서드

---
## 19) Redis Key 설계

| Key 패턴 | Value 타입 | 설명 |
|---|---|---|
| `course:capacity:{courseId}` | String | 강좌 잔여 정원 |
| `course:queue:{courseId}` | Sorted Set | 신청자 목록 (score = 신청시각 ms) |
| `user:courses:{studentNo}` | String (JSON) | 사용자의 수강 신청 목록 캐시 (TTL 5분) |

---
## 20) QueueController

### (1) Attributes
- `getQueueStatusUseCase: GetQueueStatusUseCase` : 대기열 상태 조회 유스케이스

### (2) Methods
- `+getQueueRank(Long courseId, String studentNo): BaseResponse` : 특정 강좌에서 학생의 대기열 순위를 조회하여 반환

---
## 21) QueueService

### (1) Attributes
- `registrationRedisPort: RegistrationRedisPort` : Redis 대기열 연산을 위한 포트
- `registrationValidator: RegistrationValidator` : 수강 신청 유효성 검증기

### (2) Methods
- `+getQueueStatus(String studentNo, Long courseId): QueueStatusResponse` : 학생의 대기열 상태(순위, 대기열 크기 등) 조회

---
# 3. Sequence diagram

---
## 1. 회원 가입

<img width="700" height="421" alt="Image" src="https://github.com/user-attachments/assets/e0b5a161-a694-49fd-b8b2-4b44b4f8992e" />

위의 그림은 사용자가 시스템에 회원 가입을 수행하는 과정을 표현하는 Sequence Diagram이다. 사용자가 회원 가입 폼에 정보를 입력하고 가입을 요청하면, `UserService`가 작동하여 `UserPersistenceAdapter`를 통해 로컬 DB에 회원 정보를 저장(Save)하며, 저장이 성공적으로 완료되면 사용자에게 가입 완료 메시지를 반환한다.

---
## 2. 로그인 

<img width="700" height="421" alt="Image" src="https://github.com/user-attachments/assets/3d0966b1-79c4-46b2-be3f-8e3ac54b7b87" />

위의 그림은 등록된 회원이 시스템에 로그인을 수행하는 과정을 표현하는 Sequence Diagram이다. 사용자가 학번과 비밀번호를 입력하여 로그인을 요청하면, `UserService`는 `UserPersistenceAdapter`에서 해당 사용자의 정보를 조회(Find)한다. 조회된 정보와 입력된 비밀번호가 일치하면 로그인 완료 메시지를 사용자에게 반환한다.

---
## 3. 강의 생성

<img width="937" height="431" alt="Image" src="https://github.com/user-attachments/assets/f573f1ac-f747-494f-95fe-494017426fce" />

위의 그림은 관리자 또는 강사가 새로운 강의를 등록하는 과정을 표현하는 Sequence Diagram이다. 사용자가 강의 생성 버튼을 클릭하고 강의 정보를 입력하여 요청하면, `CourseService`는 전달받은 데이터를 바탕으로 `Course`객체를 생성한다. 생성된 강의 정보는 `CoursePersistenceAdapter`를 통해 데이터베이스에 저장(Save)되며, 등록이 완료되면 강의 생성 완료 메시지를 출력한다.

---
## 4. 강의 조회

<img width="701" height="361" alt="Image" src="https://github.com/user-attachments/assets/e2f8db18-21b3-429f-af3f-5949f900a1d7" />

위의 그림은 사용자가 시스템에 개설된 강의 목록을 확인하는 과정을 표현하는 Sequence Diagram이다. 사용자가 강의 목록 조회를 요청하면, `CourseService`는 `CoursePersistenceAdapter`에 전체 강의 데이터 조회를 요청(Find All)한다. `CoursePersistenceAdapter`가 데이터베이스로부터 강의 리스트를 가져와 반환하면, `CourseService`는 이 목록을 사용자 화면에 출력하여 보여준다.

---
## 5. 강의 삭제

<img width="868" height="411" alt="Image" src="https://github.com/user-attachments/assets/09e41f14-8a34-4852-a7d9-e5b0516d2f86" />

위의 그림은 등록된 강의를 시스템에서 삭제하는 과정을 표현하는 Sequence Diagram이다. 사용자가 특정 강의의 삭제를 요청하면, `CourseService`는 `CoursePersistenceAdapter`를 호출하여 권한을 확인 한 다음 해당 강의 데이터의 삭제(Delete)를 수행한다. 데이터베이스에서 삭제 처리가 안전하게 완료되면, 사용자에게 강의 삭제 완료 메시지를 반환한다.

---
## 6. 수강 신청

<img width="1673" height="1593" alt="Image" src="https://github.com/user-attachments/assets/30b38a96-e0c9-445e-a580-d2e8893d5747" />

위의 그림은 사용자가 특정 강의에 수강 신청하는 과정을 표현하는 Sequence Diagram이다. 사용자가 수강 신청을 요청하면 `RegistrationService`는 `RegistrationValidator`를 통해 사용자(USER_NOT_FOUND), 강의(COURSE_NOT_FOUND) 존재 여부를 순차적으로 검증하고, 강의 수강 신청 가능 상태(COURSE_TERMINATED) 및 시간대 충돌 여부(TIME_CONFLICT)를 추가로 확인한다. 각 단계에서 조건이 충족되지 않을 경우 해당 에러 코드를 즉시 반환하고 흐름을 종료한다.
모든 사전 검증이 완료되면 `availableCapacity = course.maxCapacity - course.currentEnrolled`를 계산한 뒤, `RegistrationRedisPersistenceAdapter`를 통해 Redis에 Lua 스크립트(JOIN_SCRIPT)를 원자적으로 실행한다. Lua 스크립트는 중복 신청 여부를 `ZSCORE`로 먼저 확인하고, 잔여 정원(`course:capacity:{courseId}`)을 DECR하여 정원 초과 여부를 검증한 뒤, 통과 시 신청 시각을 score로 하여 `ZADD course:queue:{courseId}`로 수강자를 `Sorted Set`에 등록하고 `ZRANK로 0-based` 순번을 반환한다. 반환값이 -3이면 이미 수강신청된 강의(REGISTRATION_ALREADY_EXISTS), -1이면 정원 초과(COURSE_FULL) 에러를 즉시 반환한다.

Redis 등록에 성공하면 `runCatching` 블록 내에서 수강 신청 기록을 인메모리 저장소에 저장하고 `CourseEnrollmentManager`를 통해 수강 인원을 증가시킨다. 저장 중 예외가 발생할 경우 .onFailure 보상 트랜잭션으로 `removeFromQueue`를 호출하여 `Redis Sorted Set`에서 해당 학생을 제거하고 정원을 원복한다. 최종적으로 `invalidateRegistrationCache`를 통해 해당 사용자의 수강 내역 캐시(`DEL user:courses:{studentNo}`)를 무효화하고, `QueueJoinResponse(immediate=true, rank=rank+1)`를 반환한다.

---
## 7. 수강 신청 내역 조회

<img width="1222" height="992" alt="Image" src="https://github.com/user-attachments/assets/0782b699-bd44-4842-b7d3-17e5c89b926b" />

위의 그림은 사용자가 자신의 수강 신청 내역을 조회하는 과정을 표현하는 Sequence Diagram으로, `Cache-Aside` 패턴이 적용되어 있다. 사용자가 조회를 요청하면 `RegistrationService`는 가장 먼저 `RegistrationRedisPersistenceAdapter`를 통해 Redis에서 캐시 데이터(user:courses:{studentNo}) 존재 여부를 확인한다. `Cache HIT` 시에는 Redis에서 반환된 데이터를 즉시 사용자에게 전달하며 DB 조회를 생략한다. `Cache MISS` 시에는 `RegistrationValidator`를 통해 사용자 존재 여부를 검증하고(USER_NOT_FOUND 에러 처리 포함), `RegistrationPersistenceAdapter`에서 원본 수강 목록을 조회한 뒤, 조회한 데이터를 Redis에 캐싱(SET ... EX 300)하여 이후 요청에 대비하고 최종 결과를 사용자에게 반환한다.

---
## 8. 수강 신청 취소

<img width="1664" height="1352" alt="Image" src="https://github.com/user-attachments/assets/e425a73f-46d8-4d4f-8f2f-b91186ecd45b" />

위의 그림은 사용자가 자신의 수강 신청을 취소하는 과정을 표현하는 Sequence Diagram이다. 사용자가 취소를 요청하면 `RegistrationService`는 `RegistrationValidator`를 통해 사용자(USER_NOT_FOUND), 강의(COURSE_NOT_FOUND), 수강 신청 내역(REGISTRATION_NOT_FOUND) 존재 여부를 순차적으로 검증하며, 각 단계에서 데이터가 존재하지 않을 경우 해당 에러 코드를 즉시 반환하고 흐름을 종료한다. 모든 검증을 통과하면 인메모리 저장소에서 수강 신청 기록을 삭제하고, `CourseEnrollmentManager`를 통해 수강 인원을 감소시킨다. 이후 `RegistrationRedisPersistenceAdapter`를 통해 Redis에 Lua 스크립트로 정원을 원자적으로 복구(INCR course:capacity)하고 수강자 집합에서 제거(SREM course:registered)한 뒤, 해당 사용자의 수강 내역 캐시(DEL user:courses:{studentNo})를 무효화하여 데이터 정합성을 보장하고 최종 완료 메시지를 반환한다.

---
## 9. 대기열 큐 랭킹 조회

<img width="1292" height="1142" alt="Image" src="https://github.com/user-attachments/assets/8f3a2d19-4ba6-4125-8228-8433f4ae0e70" />

사용자가 자신의 수강 신청 대기 순번을 조회하는 과정을 표현하는 Sequence Diagram이다. 클라이언트가 GET /registration/queue/{courseId}/rank?studentNo=xxx 요청을 보내면 QueueController는 QueueService의 getQueueStatus()를 호출한다. QueueService는 RegistrationValidator를 통해 사용자(USER_NOT_FOUND)와 강의(COURSE_NOT_FOUND) 존재 여부를 순차적으로 검증하며, 각 단계에서 데이터가 존재하지 않을 경우 해당 에러 코드를 즉시 반환하고 흐름을 종료한다.
모든 검증을 통과하면 `RegistrationRedisPersistenceAdapter`를 통해 `Redis Sorted Set`에 `ZRANK course:queue:{courseId}` 명령을 실행하여 해당 학생의 0-based 순번을 조회한다. 이후 두 가지 분기로 처리된다. 조회 결과가 null인 경우 해당 학생이 대기열에 존재하지 않는 것으로 간주하여 즉시 확정 상태인 `QueueStatusResponse(status=CONFIRMED)`를 반환한다. 반환값이 존재하는 경우에는 추가로 `ZCARD course:queue:{courseId}` 명령으로 전체 대기 인원 수를 조회한 뒤, 0-based 순번에 1을 더한 1-based 순번과 전체 인원을 포함한 `QueueStatusResponse(status=CONFIRMED, rank=rank+1, totalInQueue=total)`를 반환하고 최종적으로 응답한다.

---
# 4. State machine diagram

<img width="1343" height="1221" alt="Image" src="https://github.com/user-attachments/assets/bf7e6a09-de0f-48fe-a8da-660a5a70c3a9" />

수강신청 시스템은 수강신청(Registration), 캐시(Cache), 강의(Course) 세 가지 State Machine으로 구성된다.

---
## 수강신청 State Machine
수강신청 State Machine에서 학생은 초기에 미신청(UNREGISTERED) 상태에 있다. register()를 호출하면 검증 중(VALIDATING) 상태로 전이되어 사용자 존재 여부, 강의 존재 여부, 강의 마감 여부, 시간표 충돌 여부를 순차적으로 검증한다. 검증에 실패하면 존재하지 않는 회원, 존재하지 않는 강의, 이미 마감된 강의, 기존 수강 강의와의 시간표 충돌 중 해당하는 예외와 함께 미신청 상태로 돌아간다. 모든 검증을 통과하면 Redis 처리 중(Lua JOIN_SCRIPT) 상태로 전이되어 Lua 스크립트를 통해 중복 신청 검사, 잔여 정원 확인, 정원 차감, Sorted Set 등록, 순번 반환을 원자적으로 수행한다. 정원이 초과되거나(-1L) 이미 해당 강의에 신청된 경우(-3L) 미신청 상태로 돌아간다. rank ≥ 0으로 Redis 처리가 성공하면 DB 저장 중(runCatching) 상태로 전이되어 수강신청 정보를 저장하고 수강 인원을 증가시키며 캐시를 무효화한다. DB 저장에 실패하면 Redis에 등록된 데이터를 원복(removeFromQueue, ZREM + INCR)하고 미신청 상태로 돌아간다. 저장이 완료되면 수강 확정(CONFIRMED) 상태가 되어 QueueJoinResponse(immediate=true, rank=rank+1)를 반환한다. 수강 확정 상태에서 cancelRegistration()을 호출하면 취소 처리 중(CANCELLING) 상태로 전이되어 DB 삭제, 수강 인원 감소, Redis 큐 제거(ZREM + INCR), 캐시 삭제(DEL)를 수행한 뒤 취소 완료 응답을 반환하며 다시 미신청 상태로 돌아간다. 수강 확정 상태를 그대로 유지하는 경우 최종 상태(Final State)로 전이되어 수명이 종료된다.

---
## 캐시 State Machine
캐시 State Machine에서 초기 상태는 캐시 없음(Cache MISS)이며, DB를 직접 조회(findByUserId)하여 RegistrationResponse로 변환한 뒤 cacheRegistrations()를 호출하면 캐시 있음(Cache HIT) 상태로 전이된다. 캐시는 user:courses:{studentNo} 키에 5분 TTL(Duration.ofMinutes(5))로 저장된다. 수강신청 또는 취소 시 invalidateRegistrationCache()가 호출되면 해당 키를 DEL하고 캐시 없음 상태로 돌아간다.

---
## 강의 State Machine
강의 State Machine에서 강의는 초기에 수강신청 가능(OPEN) 상태이며 register() 호출을 허용한다. 정원이 초과되면 마감됨(TERMINATED) 상태로 전이되어 이후 validateCourseIsOpen() 검증 시 COURSE_TERMINATED 예외가 발생한다.

# 5. Glossary

| 용어 (Term) | 정의 (Definition) |
| :--- | :--- |
| **TPS** | Transaction Per Second의 약자로, 1초당 시스템이 처리하는 트랜잭션의 수. |
| **Lua Script** | Redis에서 원자적으로 실행되는 스크립트. 여러 Redis 명령을 하나의 트랜잭션처럼 처리하여 Race Condition을 방지한다. |
| **Sorted Set (ZSET)** | Redis의 자료구조로, 각 멤버에 score를 부여해 정렬된 순서를 유지한다. 본 시스템에서 대기열 순번 관리에 사용된다. |
| **Race Condition** | 두 개 이상의 요청이 동시에 공유 자원에 접근할 때 실행 순서에 따라 결과가 달라지는 문제. |
| **Cache Hit / Miss** | 요청한 데이터가 캐시에 존재하면 Hit, 존재하지 않으면 Miss. Miss 시 DB를 직접 조회한다. |
| **TTL (Time To Live)** | 캐시 데이터의 유효 시간. 만료되면 자동으로 삭제된다. 본 시스템에서는 5분으로 설정된다. |
| **Cache Invalidation** | 데이터 변경 시 캐시를 삭제하여 일관성을 유지하는 과정. |
| **Queue (대기열)** | 수강신청 요청을 순서대로 처리하기 위한 대기 구조. |
| **Rank (순번)** | Sorted Set에서 ZRANK 명령으로 반환되는 0-based 순위. 사용자에게는 +1하여 1-based로 전달한다. |
| **Polling (폴링)** | 클라이언트가 일정 주기로 서버에 대기 순번을 반복 요청하는 방식. |

> # 6. References

강의자료 참고
- [OSSDesign] 06_Structural Modeling I
- [OSSDesign] 07_Structural Modeling II
- [OSSDesign] 08_Behavior modeling I
- [OSSDesign] 09_Behavior Modeling II